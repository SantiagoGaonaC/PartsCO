<%- include("./template/head.ejs") %>
    <meta name="description" content="">
    <title>Editar User Admin</title>
    <link href="css/signin.css" rel="stylesheet">
  </head>

  <body class="text-center">
    <% if (usuarios) { %>
    <div>
      <div class="position-absolute top-50 start-50 translate-middle">
        <main class="form-signin text-center shadow-lg p-2 mb-5">
          <form class="row g-3" id="formLogin" action="/update" method="POST">
            <h1 class="h3 mb-3 fw-normal"><strong>Editar PartsCO</strong></h1>

            <input id="id" name="id" value="<%= usuarios.id %>" hidden>

            <div class="position-relative">
              <label for="nombre" class="form-label">Nombre</label>
              <input type="text" name="nombre" class="form-control" id="nombre" value="<%= usuarios.nombre  %>">
          </div>
          
          <div class="position-relative">
            <label for="apellido" class="form-label">Apellido</label>
            <input type="text" name="apellido" class="form-control" id="apellido" value="<%= usuarios.apellido  %>">
          </div>
        
          <div class="position-relative">
            <label for="email" class="form-label">Email</label>
            <input type="text" name="email" class="form-control" id="email" value="<%= usuarios.email  %>">
          </div>

          <div class="position-relative">
            <label for="rol" class="form-label">Rol</label>
            <select class="form-select" name="rol" id="rol" tabindex="3">
                <option <%= usuarios.rol=='usuario' ? "selected" : '' %> value="usuario">Usuario</option>
                <option <%= usuarios.rol=='empleado' ? "selected" : '' %> value="empleado">Empleado</option>
                <option <%= usuarios.rol=='admin' ? "selected" : '' %> value="admin">Admin</option>
              </select>
          </div>
        
          <div class="col-12">
            <button class="w-100 btn btn-primary fw-bold" type="submit">Enviar</button>
          </div>
          </form>
            <br>
            <a href="/admin" type="button" class="w-100 btn btn-primary fw-bold">Panel Admin</a>
            <br>
            <p class="mt-1 mb-3 text-muted">&copy;Proyecto integrador PartsCO 2022-1</p>
        </main>
      </div>
    
    </div>
    <% } %>
    <% if(alert){ %>
      <script>
        Swal.fire({
          title: '<%=alertTitle%>',
          text: '<%=alertMessage%>',
          icon: '<%=alertIcon%>',
          showConfirmButton: <%=showConfirmButton%>,
          timer: <%=timer%>
        }).then(()=>{
          window.location = '/<%=ruta%>'
        })
      </script>
    <% } %>

  </body>
</html>
